name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: sudo apt install python3-pip
    steps:
      - name: my-step2
        run: echo 'Hello'    
        